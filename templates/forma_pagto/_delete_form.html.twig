<form method="post" action="{{ path('forma_pagto_delete', {'id': forma_pagto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forma_pagto.id) }}">
    <button class="btn">Delete</button>
</form>
