{% extends 'base.html.twig' %}

{% block title %}Edit Vendas{% endblock %}

{% block body %}
    <h1>Edit Vendas</h1>

    {{ include('vendas/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('vendas_index') }}">back to list</a>

    {{ include('vendas/_delete_form.html.twig') }}
{% endblock %}
