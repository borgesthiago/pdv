{% extends 'base.html.twig' %}

{% block title %}TipoVenda index{% endblock %}

{% block body %}
    <h1>TipoVenda index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descricao</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tipo_venda in tipo_vendas %}
            <tr>
                <td>{{ tipo_venda.id }}</td>
                <td>{{ tipo_venda.descricao }}</td>
                <td>
                    <a href="{{ path('tipo_venda_show', {'id': tipo_venda.id}) }}">show</a>
                    <a href="{{ path('tipo_venda_edit', {'id': tipo_venda.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('tipo_venda_new') }}">Create new</a>
{% endblock %}
